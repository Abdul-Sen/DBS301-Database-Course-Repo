CREATE OR REPLACE VIEW EMPVU50 AS
SELECT LAST_NAME, JOB_ID, SALARY
FROM EMPLOYEES
WHERE DEPARTMENT_ID = 50
ORDER BY LAST_NAME;

SELECT * FROM EMPVU50;

/*Below will not work because */
INSERT INTO EMPVU50 VALUES (300,'ABDUL',2000);

/*===========================*/
CREATE OR REPLACE VIEW SAL80_VU AS
(SELECT LAST_NAME, JOB_ID, SALARY*12 "ANNSAL"
FROM EMPLOYEES
);